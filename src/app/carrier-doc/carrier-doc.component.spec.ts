import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CarrierDocComponent } from './carrier-doc.component';
/*
describe('CarrierDocComponent', () => {
  let component: CarrierDocComponent;
  let fixture: ComponentFixture<CarrierDocComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CarrierDocComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CarrierDocComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/